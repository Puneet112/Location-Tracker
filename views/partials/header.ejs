<!DOCTYPE html>
<html lang="en">
  <head>
      <meta charset="utf-8">
      <meta http-equiv="X-UA-Compatible" content="IE=edge">
      <meta name="viewport" content="width=device-width, initial-scale=1">
      <title>Location Tracker</title>
      <link rel="stylesheet" href="/stylesheets/bootstrap.min.css">
      <link rel="stylesheet" href="https://use.fontawesome.com/releases/v5.11.2/css/all.css">
      <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/4.0.0/css/bootstrap.min.css"
        integrity="sha384-Gn5384xqQ1aoWXA+058RXPxPg6fy4IWvTNh0E263XmFcJlSAwiGgFAW/dAiS6JXm" crossorigin="anonymous">
      <link rel="stylesheet" href="/stylesheets/main.css">
      <link rel="stylesheet" href="/stylesheets/animate.css">
      <script src="/socket.io/socket.io.js"></script>
  </head>
  <body>
    <div class="content">
      <div class="inner-content">
        <nav class="navbar navbar-expand-lg navbar-dark bg-dark sticky-top">
          <div class="nav-display">
            <a class="navbar-brand" href="/">
              <img class="logo" src="/images/logo.png" width="65" height="55" alt="">
              Location Tracker
            </a>
            <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent"
              aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
              <span class="navbar-toggler-icon"></span>
            </button>
            
            <div class="collapse navbar-collapse" id="navbarSupportedContent">
              <ul class="navbar-nav ml-auto">
                
              <% if(!currentUser) {  %>
                    <li class="nav-item ">
                      <a class="nav-link button-nav" href="/login">Login <span class="sr-only">(current)</span></a>
                    </li>
                    <li class="nav-item">
                      <a class="nav-link button-nav" href="/register">Sign Up</a>
                    </li>
                <%} else  { %>
                    <li class="nav-item dropdown notification">
                      <a class="nav-link btn btn-outline-light dropdown-toggle" id="user-style" href="#" id="dropdownMenuButton" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                        <span id="user"><%=currentUser.username%></span>
                        <i class="fas fa-user"></i>
                        <span class="badge badge-warning" id="warnings"></span>
                      </a>
                      <div class="dropdown-menu menu-container" aria-labelledby="dropdownMenuButton">
                        <h5>Notifications</h5>
                        <hr />
                        <div id="notification-list">
                        </div>
                      </div>
                    </li>
                    <li class="nav-item">
                      <a class="nav-link button-nav" href="/logout">Logout</a>
                    </li>
                    <li class="nav-item">
                      <a class="nav-link button-nav" href="/locate">Locate Devices</a>
                    </li>
                <% } %>
                
                <li class="nav-item ">
                    <a class="nav-link button-nav" href="#"> Support<span class="sr-only"></span></a>
                </li>
                
              </ul>
            </div>
          </div>
          
        </nav>
        <div id="alert-text">
          <% if(error && error.length > 0) { %>
          <div class="alert alert-danger">
            <p><%=error %></p>
          </div>
          <% } %>
        
          <% if(success && success.length > 0) { %>
          <div class="alert alert-success">
            <p><%= success %></p>
          </div>
          <% } %>
        </div>
  
 
